export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <html lang="en">
        <body style={{ margin: 0, padding: 0, width: '100%', height: '100%' }}>
          {children}
        </body>
      </html>
    </html>
  );
}
